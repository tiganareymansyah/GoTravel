import { Box } from "@mui/material";

export const listDataBooking = (
    classes, 
    props 
) => {
    console.log(props);

    const style = {};

    return (
        <Box>List Data Booking</Box>
    );
};
