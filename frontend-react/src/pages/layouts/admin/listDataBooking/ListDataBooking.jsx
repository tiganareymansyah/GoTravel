import { Box } from "@mui/material";

export default function ListDataBooking ({
    props
}) {
    console.log(props);

    const style = {};

    return (
        <Box>List Data Booking</Box>
    );
};
